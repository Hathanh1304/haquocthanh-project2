@model IEnumerable<PROJECT2.Models.SanBay>

@{
    ViewBag.Title = "Danh sach san bay";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Danh sach san bay</h2>

<p>
    @*Html.ActionLink("Create New", "Create")*@
</p>
<a href="/SanBays/create" class="btn btn-primary my-3"> Them moi</a>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenSanBay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ThanhPho)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuocGia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaSanBayKyHieu)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenSanBay)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThanhPho)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuocGia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaSanBayKyHieu)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.MaSanBay }) |
                @Html.ActionLink("Details", "Details", new { id = item.MaSanBay }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.MaSanBay })
            </td>
        </tr>
    }

</table>
